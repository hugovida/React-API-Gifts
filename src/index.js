import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; 
import GifGrid from './GridGift';

ReactDOM.render(
  <React.StrictMode>
    <GifGrid />
  </React.StrictMode>,
  document.getElementById('root')
);